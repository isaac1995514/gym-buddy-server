import firebase from "firebase";
import firebaseDb from "../firebaseDb";

/**
 * Get a list of exercise
 * @param {*} req
 * @param {*} res
 * @param {*} next
 */
export const allExercise_Get = (req, res, next) => {
  // Filter by
  // 1. Muscle Category
  // 2. Muscle
};

/**
 * Create a new exercise
 * @param {*} req
 * @param {*} res
 * @param {*} next
 */
export const createExercise_Post = (req, res, next) => {};

/**
 * Delete an existing exercise
 * @param {*} req
 * @param {*} res
 * @param {*} next
 */
export const deleteExercise_Post = (req, res, next) => {};

/**
 * Update an existing exercise
 * @param {*} req
 * @param {*} res
 * @param {*} next
 */
export const updateExercise_Post = (req, res, next) => {};
